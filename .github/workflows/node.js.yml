name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]

    # 🧱 Thêm MongoDB thật làm service chạy song song
    services:
      mongodb:
        image: mongo:6.0
        ports:
          - 27017:27017
        # ✅ Sửa healthcheck dùng mongosh (cách 1 theo gợi ý của Copilot)
        options: >-
          --health-cmd "mongosh --quiet --eval \"db.adminCommand({ ping: 1 })\""
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10

    steps:
      - uses: actions/checkout@v4

      # -------------------------------------------------------
      # 1️⃣ Cấu hình Node.js cho toàn pipeline
      # -------------------------------------------------------
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      # -------------------------------------------------------
      # 2️⃣ restaurant-service: setup cache + install + test
      # -------------------------------------------------------
      - name: Setup Node.js cache for restaurant-service
        uses: actions/cache@v4
        with:
          path: ./backend/restaurant-service/node_modules
          key: ${{ runner.os }}-restaurant-${{ matrix.node-version }}-${{ hashFiles('./backend/restaurant-service/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-restaurant-${{ matrix.node-version }}-

      - name: Install dependencies for restaurant-service
        working-directory: ./backend/restaurant-service
        run: npm ci

      # 🧩 Chạy test với MongoDB thật (thông qua biến môi trường)
      # ✅ Có thể thêm bước chờ MongoDB nếu cần (tùy chọn)
      - name: Run tests for restaurant-service
        working-directory: ./backend/restaurant-service
        env:
          MONGODB_URI: mongodb://localhost:27017/testdb
        run: npm test

      # -------------------------------------------------------
      # 3️⃣ frontend: setup cache + install + test
      # -------------------------------------------------------
      - name: Setup Node.js cache for frontend
        uses: actions/cache@v4
        with:
          path: ./frontend/node_modules
          key: ${{ runner.os }}-frontend-${{ matrix.node-version }}-${{ hashFiles('./frontend/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-frontend-${{ matrix.node-version }}-

      - name: Install dependencies for frontend
        working-directory: ./frontend
        run: npm ci

      - name: Run tests for frontend
        working-directory: ./frontend
        run: npm test
