version: '3.8'
services:
  restaurant-service:
    build:
      context: ./restaurant-service
    ports:
      - "6002:5002"
    environment:
      - MONGODB_URI=mongodb://mongo:27017/restaurant
    depends_on:
      - mongo

  mongo:
    image: mongo:6.0
    ports:
      - "27017:27017"